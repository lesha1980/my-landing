nav.nav
    -var arr = ['Home', 'About us', 'Our Approach', 'Our Work']
    each item in arr
        if item === 'Home'
            a.nav__link.nav__link--active(href='#')=item
        else
            a.nav__link(href='#')=item